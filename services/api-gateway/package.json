{
  "name": "cloudach-api-gateway",
  "version": "1.0.0",
  "description": "OpenAI-compatible API gateway for Cloudach — proxies to vLLM with auth and rate limiting",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.3",
    "pg": "^8.12.0",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^10.0.0",
    "pino": "^9.3.2",
    "pino-http": "^10.3.0"
  },
  "devDependencies": {
    "eslint": "^9.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
